# Blueprint Render: https://render.com/docs/infrastructure-as-code
#
# omeclone-api: tras el primer deploy, en el panel añade DATABASE_URL (Neon) si sync: false.
# omeclone-web: añade VITE_BACKEND_URL = URL pública del API (HTTPS, sin barra final).

services:
  - type: web
    name: omeclone-api
    runtime: docker
    dockerfilePath: ./backend/Dockerfile
    dockerContext: backend
    plan: free
    healthCheckPath: /health
    envVars:
      - key: DATABASE_URL
        sync: false
      - key: SECRET_KEY
        generateValue: true

  - type: web
    name: omeclone-web
    runtime: static
    rootDir: frontend
    buildCommand: npm ci && npm run build:cloudflare
    staticPublishPath: ./frontend/dist
    envVars:
      - key: NODE_VERSION
        value: "20"
      - key: VITE_BACKEND_URL
        sync: false
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
